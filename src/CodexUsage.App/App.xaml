<Application
    x:Class="CodexUsage.App.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CodexUsage.App.Controls"
    RequestedTheme="Dark">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>
            <controls:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <SolidColorBrush x:Key="AppBackgroundBrush" Color="#0D1218" />
            <SolidColorBrush x:Key="TitleBarBrush" Color="#0A0F15" />
            <SolidColorBrush x:Key="SurfaceBrush" Color="#111922" />
            <SolidColorBrush x:Key="SubtleSurfaceBrush" Color="#0D1218" />
            <SolidColorBrush x:Key="ElevatedSurfaceBrush" Color="#1C2A3C" />
            <SolidColorBrush x:Key="InputSurfaceBrush" Color="#0A0F15" />
            <SolidColorBrush x:Key="TableHeaderBrush" Color="#111922" />
            <SolidColorBrush x:Key="TableDividerBrush" Color="#273444" />
            <SolidColorBrush x:Key="CardBorderBrush" Color="#35465A" />
            <SolidColorBrush x:Key="ControlBorderBrush" Color="#35465A" />
            <SolidColorBrush x:Key="SelectedSurfaceBrush" Color="#1C2A3C" />
            <SolidColorBrush x:Key="SelectedBorderBrush" Color="#5B91E8" />
            <SolidColorBrush x:Key="PrimaryBrush" Color="#5B91E8" />
            <SolidColorBrush x:Key="PrimaryFillBrush" Color="#5B91E8" />
            <SolidColorBrush x:Key="CoverageBrush" Color="#63C5A6" />
            <SolidColorBrush x:Key="SuccessBrush" Color="#63C5A6" />
            <SolidColorBrush x:Key="WarningBrush" Color="#E2A44F" />
            <SolidColorBrush x:Key="DangerBrush" Color="#CF697C" />
            <SolidColorBrush x:Key="PurpleBrush" Color="#8C78D7" />
            <SolidColorBrush x:Key="CostUncachedBrush" Color="#5B91E8" />
            <SolidColorBrush x:Key="CostCachedBrush" Color="#63C5A6" />
            <SolidColorBrush x:Key="CostReasoningBrush" Color="#E2A44F" />
            <SolidColorBrush x:Key="CostOtherBrush" Color="#8C78D7" />
            <SolidColorBrush x:Key="PrimaryTextBrush" Color="#E8EDF3" />
            <SolidColorBrush x:Key="SecondaryTextBrush" Color="#9EABBA" />
            <SolidColorBrush x:Key="TertiaryTextBrush" Color="#8290A3" />
            <SolidColorBrush x:Key="ProgressTrackBrush" Color="#35465A" />

            <Style x:Key="CardStyle" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource TableDividerBrush}" />
                <Setter Property="BorderThickness" Value="0,1" />
                <Setter Property="CornerRadius" Value="0" />
            </Style>

            <Style x:Key="InsetCardStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
                <Setter Property="Background" Value="{StaticResource SubtleSurfaceBrush}" />
            </Style>

            <Style x:Key="FilterToggleButtonStyle" TargetType="ToggleButton">
                <Setter Property="Background" Value="{StaticResource InputSurfaceBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource ControlBorderBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="3" />
                <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}" />
                <Setter Property="UseSystemFocusVisuals" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Grid
                                x:Name="ToggleRoot"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                                <ContentPresenter
                                    x:Name="ToggleContent"
                                    Padding="{TemplateBinding Padding}"
                                    HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    Foreground="{TemplateBinding Foreground}" />
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CheckStates">
                                        <VisualState x:Name="Unchecked" />
                                        <VisualState x:Name="Checked">
                                            <VisualState.Setters>
                                                <Setter Target="ToggleRoot.Background" Value="{StaticResource SelectedSurfaceBrush}" />
                                                <Setter Target="ToggleRoot.BorderBrush" Value="{StaticResource SelectedBorderBrush}" />
                                                <Setter Target="ToggleContent.Foreground" Value="{StaticResource PrimaryTextBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Indeterminate">
                                            <VisualState.Setters>
                                                <Setter Target="ToggleRoot.Background" Value="{StaticResource SelectedSurfaceBrush}" />
                                                <Setter Target="ToggleRoot.BorderBrush" Value="{StaticResource SelectedBorderBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
                <Setter Property="FontSize" Value="18" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </Style>

            <Style x:Key="CaptionStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource TertiaryTextBrush}" />
                <Setter Property="FontSize" Value="12" />
            </Style>

            <Style x:Key="CommandHeaderTextStyle" TargetType="TextBlock">
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="FontFamily" Value="{StaticResource ContentControlThemeFontFamily}" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="FontWeight" Value="Normal" />
                <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}" />
            </Style>

            <Style x:Key="MetricValueStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
                <Setter Property="FontSize" Value="26" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </Style>

            <Style x:Key="TableHeaderTextStyle" TargetType="TextBlock">
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </Style>

            <Style x:Key="TableCellTextStyle" TargetType="TextBlock">
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
                <Setter Property="FontSize" Value="14" />
            </Style>

            <Style x:Key="PriceCellTextStyle" TargetType="TextBlock" BasedOn="{StaticResource TableCellTextStyle}">
                <Setter Property="FontSize" Value="15" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </Style>

            <Style TargetType="ProgressBar">
                <Setter Property="Height" Value="6" />
                <Setter Property="Background" Value="{StaticResource ProgressTrackBrush}" />
                <Setter Property="CornerRadius" Value="3" />
            </Style>

            <Style TargetType="Button">
                <Setter Property="CornerRadius" Value="4" />
                <Setter Property="MinHeight" Value="40" />
            </Style>

            <Style TargetType="ToggleButton">
                <Setter Property="CornerRadius" Value="3" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
